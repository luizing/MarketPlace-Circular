spring.application.name=marktplaceCircular
spring.profiles.default=postgres
spring.config.import=optional:file:.env[.properties]
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET}
jwt.expiracao-ms=3600000
